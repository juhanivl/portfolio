import React, { Component } from 'react';
import { Router, Route, Link, IndexRoute, hashHistory, browserHistory , IndexLink} from 'react-router'

export default class Projects extends Component {
  /*
  Add the following JavaScript events and
  react to them accordingly by showing a message
  or changing something in your page:
   Load
   Scroll
   Mouse over your first project
  */
  render(){
    return(
      <div>

        <h1>Projects</h1>

    </div>
    );
  }
}
